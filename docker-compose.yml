version: "3.9"

services:
  app:
    build:
      context: .
    container_name: capstone2_app
    ports:
      - "8000:8000"
    env_file:
      - ./.env
    volumes:
      - uploads_data:/app/uploads
    restart: always

volumes:
  uploads_data:
