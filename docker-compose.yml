version: "3.9"

services:
  app:
    build:
      context: ./capstone2
    container_name: capstone2_app
    ports:
      - "80:8000"
    env_file:
      - ./capstone2/.env
    volumes:
      - uploads_data:/app/uploads
    restart: always

volumes:
  uploads_data:
