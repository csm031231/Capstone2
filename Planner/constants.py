"""
공유 상수 모음

여러 서비스에서 중복 정의되던 매직넘버/문자열을 한 곳에 모아 관리합니다.
값을 바꿀 때 이 파일만 수정하면 됩니다.
"""
from datetime import time

# ---------------------------------------------------------------------------
# 지역명 정규화
# 광역 지역명(전라도 등)은 DB address 컬럼에 "전라남도"/"전라북도"처럼 저장되어
# 있어 LIKE 검색이 바로 안 됩니다. 공통 접두어로 변환하여 contains() 검색에 사용.
# ---------------------------------------------------------------------------
REGION_PREFIX: dict = {
    # 도 그룹 (여러 도를 하나로 묶을 때)
    "전라도": "전라",
    "경상도": "경상",
    "충청도": "충청",
    "강원도": "강원",
    "제주도": "제주",
    # 특별시/광역시 (주소가 직접 시명으로 시작 → 접두 LIKE 안전)
    "서울": "서울",
    "부산": "부산",
    "대구": "대구",   # '대구로' 같은 도로명 오탐 방지를 위해 접두 매칭 유지
    "인천": "인천",
    "광주": "광주",   # 광주광역시 우선 (경기 광주시 혼재 가능하나 일반적 입력 기준)
    "대전": "대전",
    "울산": "울산",
    "세종": "세종",
    # 도 단위 약칭 (주소가 '경기도', '강원특별자치도' 등으로 시작)
    "경기": "경기",
    "경기도": "경기",
    "강원": "강원",
    "충북": "충청북",
    "충남": "충청남",
    "전북": ["전라북", "전북특별자치도"],  # 2024년 전라북도→전북특별자치도 명칭 변경 대응
    "전남": "전라남",
    "경북": "경상북",
    "경남": "경상남",
    "제주": "제주",
    # ※ 경주·전주·강릉 등 도 소속 일반 시(市)는 여기에 없으므로
    #   recommend_service / planner_service에서 contains 폴백으로 처리됨
}

# ---------------------------------------------------------------------------
# 지역 별칭 매핑
# 사진 분석(GPT Vision) 등 외부 입력에서 들어오는 영문/한문/광역명을
# DB에서 사용하는 대표 한국어 지역명으로 정규화할 때 사용합니다.
# ---------------------------------------------------------------------------
REGION_ALIASES: dict = {
    "서울": ["seoul", "서울", "서울특별시"],
    "부산": ["busan", "부산", "부산광역시"],
    "제주": ["jeju", "제주도", "제주특별자치도"],
    "강원": ["gangwon", "강원", "강원도"],
    "경기": ["gyeonggi", "경기", "경기도"],
    "인천": ["incheon", "인천", "인천광역시"],
    "대구": ["daegu", "대구", "대구광역시"],
    "광주": ["gwangju", "광주", "광주광역시"],
    "대전": ["daejeon", "대전", "대전광역시"],
    "울산": ["ulsan", "울산", "울산광역시"],
    "경북": ["gyeongbuk", "경북", "경상북도"],
    "경남": ["gyeongnam", "경남", "경상남도"],
    "전북": ["jeonbuk", "전북", "전라북도"],
    "전남": ["jeonnam", "전남", "전라남도"],
    "충북": ["chungbuk", "충북", "충청북도"],
    "충남": ["chungnam", "충남", "충청남도"],
    "세종": ["sejong", "세종", "세종특별자치시"],
}

# ---------------------------------------------------------------------------
# 요일 이름 (Python weekday() 인덱스 순서: 월=0, ..., 일=6)
# ---------------------------------------------------------------------------
WEEKDAY_KR = ["월", "화", "수", "목", "금", "토", "일"]
WEEKDAY_EN = ["mon", "tue", "wed", "thu", "fri", "sat", "sun"]

# ---------------------------------------------------------------------------
# 하루 일정 기본 시작/종료 시간
# UserPreference에 값이 없을 때 사용되는 fallback
# ---------------------------------------------------------------------------
DEFAULT_DAY_START = time(9, 0)
DEFAULT_DAY_END = time(23, 0)

# ---------------------------------------------------------------------------
# 식사 시간대 기본값
# ---------------------------------------------------------------------------
LUNCH_START = time(12, 0)
LUNCH_END = time(14, 0)
EARLY_DINNER_START = time(17, 30)  # 오후 일정이 일찍 끝나면 이 시간부터 저녁 가능
DINNER_START = time(18, 30)        # 오후 일정이 늦게 끝나면 이 시간까지 기다리지 않고 도착 시간에 먹음

# ---------------------------------------------------------------------------
# 야경/야간 장소 최소 시작 시간
# ---------------------------------------------------------------------------
NIGHT_START = time(20, 0)

# ---------------------------------------------------------------------------
# GPT max_tokens
# ---------------------------------------------------------------------------
GPT_PLANNER_MAX_TOKENS = 4096   # 일정 생성 (다일정 JSON이 크므로 여유 있게)
GPT_CHAT_MAX_TOKENS = 1000      # 채팅 수정 응답
GPT_VISION_MAX_TOKENS = 500     # 사진 분석 응답

# ---------------------------------------------------------------------------
# 채팅 히스토리 한도
# ---------------------------------------------------------------------------
CHAT_CONTEXT_LIMIT = 10   # GPT에 전달하는 최근 메시지 수 (토큰 절약)
CHAT_STORAGE_LIMIT = 20   # DB에 저장하는 최대 메시지 수
